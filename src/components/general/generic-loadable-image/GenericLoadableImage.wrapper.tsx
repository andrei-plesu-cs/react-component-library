import styled from 'styled-components';

const GenericLoadableImageWrapper = styled.div`
    position: relative;
    width: 100%;
    height: 100%;
    overflow: hidden;
`;

export default GenericLoadableImageWrapper;